import React from 'react';

import classes from './WhippedCream.scss';

const WhippedCream = ( props ) => (
     <div className={classes.WhippedCream}>WhippedCream</div>
           
);

export default WhippedCream;